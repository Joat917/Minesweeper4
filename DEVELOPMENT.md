# 开发日志

按时间顺序记下每个阶段做了什么、踩了什么坑、怎么修的。版本号对应 `index.html` 里的 `?v=N`。

---

## 阶段一 · 初始实现（v1）

四个文件：`index.html` / `css/style.css` / `js/minesweeper.js` / `js/ui.js`。

内核的硬约束是从一开始就定下的：不引用 DOM、不引用 `window`、不用 `setTimeout`、不直接用 `Math.random`。时间和随机源都靠注入，所以内核可以在 Node 里直接跑，也可以在测试里给定种子复现。

为此付出的两个代价：

- `clock` 是构造参数，`elapsedMs` 是 getter，内核不碰 `Date.now`
- `seed` 走 mulberry32，同一颗种子必然复现同一串随机数

当时还没意识到一件事：**雷图不是种子的函数**，而是 `(种子, 首点)` 的函数。这个在后面（阶段四）才暴露出来。

---

## 阶段二 · 布局返工

一次提了五条，都是可用性问题：

**计分板离中心太远。** 根因是面板被拉满 1000px 而 HUD 用 `space-between` 把两个计数器顶到两角。改成面板 `width: fit-content` 贴着棋盘，HUD 居中抱团。

**大棋盘出现滚动条。** 不再用 CSS 猜格子尺寸，新增 `fitBoard()` 按可用空间反算。关键在高度怎么算：`app 总高 − 棋盘区高` 得到"除棋盘外竖直已占掉的高度"，改格子大小不会影响这个差值，所以不会震荡。宽度取自 `.app`（定宽，不随棋盘变），也没有循环依赖。

**难度设置靠左、弹出自定义时顶宽。** 改成自定义栏常驻显示，非自定义难度下整组标灰禁用。这样工具条宽度恒定，不会被顶宽。用原生 `disabled` 而不是自己模拟禁用态。

**去掉内核说明、种子显示重复。** 删掉副标题，删掉底部独立的种子行。种子只出现在自定义栏的输入框里。

**底部橙色文字是什么。** 是 `role="status"` 状态行，我在开局时往写了"新的一局：9×9，10 颗雷"还配了橙色，确实是无意义装饰。改成默认留空，只在结算和报错时出现。

**教训**：我在状态行里塞了没人需要的信息，还给它上了强调色。UI 上每一个"看起来有意为之的装饰"都应该能被解释。

---

## 阶段三 · 缓存事故（v5）

现象：

```
Uncaught TypeError: Cannot set properties of null (setting 'textContent')
    at startGame (ui.js:71:28)
    at ui.js:491:1
```

诊断靠的是**行号对不上**：栈里说 `startGame` 第 71 行在设 `textContent`，而当时的 `ui.js` 第 71 行是空行、`startGame` 里根本没有 `textContent`；栈里调用方在第 491 行第 1 列（顶层语句），而当时的第 491 行是监听器内部的注释。

那组行号精确对上的是**上一版** `ui.js`——它里面确实有 `el.seedLabel.textContent = ...`，全文 491 行。而新版 `index.html` 按阶段二的要求删掉了 `#seedLabel`。

**旧 JS 找新 HTML 里不存在的元素 → null → 崩。**

根因是 `python -m http.server` 不发 `Cache-Control`，浏览器对 JS 做启发式缓存，普通刷新可能不去校验。

两处防御：

1. **元素自检**：模块加载时遍历 `el` 映射，缺哪个直接报"找不到 el.xxx，通常是缓存了旧脚本，请强制刷新"，而不是等某个属性在 null 上报一个看不懂的 TypeError
2. **`?v=N`**：给 `css/js` 的引用加版本参数，改完 +1

此后每次改动都会同步四处版本号：`index.html` 两处、`ui.js` 的 import、`solver.test.js` 的 import。

---

## 阶段四 · 复现上一局

用户要求实现"复现"功能，于是必须先回答一个问题：**固定种子能不能保证雷图不变？**

不能。因为"首点必定开出 0"要求落雷时避开首点的 3×3 邻域，候选池依赖首点位置，后面那串随机数的含义就全变了。

而且可以证明**两者不可兼得**：设雷图 `L` 只关于种子、与首点无关。要求"无论先点哪一格都能开出 0"，即对所有格子 `c` 都有 `adjacent[c] = 0`。假设盘上有一颗雷在 `m`，取 `m` 的任意邻格 `c`（合法首点），则 `adjacent[c] ≥ 1`，矛盾。**只要盘上有一颗雷，就一定存在某个首点翻不出 0。**

所以雷图**必须**依赖首点。结论：雷图 = `f(seed, firstIndex)`。

实现方式：内核记录 `_firstIndex`（落雷时为哪一格做的安全区），复现时把它一起带上，启动后自动补做那一次首点。走 `act()` 正常路径，所以计时、笑脸、播报的行为和玩家自己点完全一致——计时器从补首点那一刻起跑，和上一局同一口径。

**一个顺序 bug**：`startGame` 内部会 `setStatus('')`，而 `restart` 里的警告提示写在了它前面，会被立刻擦掉。改成放在之后。

---

## 阶段五 · 辅助模式的接口与 UI（v5–v6）

先只做接口和界面，求解器留空。

**设计上最关键的一点**：我发现"p < 1"和"存在合法重排"是**同一件事**。

概率定义在所有"恰好 M 颗雷、且与已翻开数字一致"的方案上。那么：

- `p < 1` ⟺ 存在一个一致方案让该格无雷 ⟺ 重排方案存在
- `p = 1` ⟺ 该格在所有一致方案里都是雷 ⟺ 重排方案不存在

所以"p = 1 才去世"不是拼上去的分支，而是数学上唯一诚实的处理。

这也让"按比例减小"有了精确含义：`综合胜率 = ∏(1 - pᵢ)`，正好等于"一个没有保命机制、纯靠运气的玩家活到现在的概率"。

内核通过构造参数接收引擎，自身不 import：

```js
new Minesweeper({ ..., assist: { analyze, resample } })
```

界面用 `isEngineReady()` 探一次引擎并缓存结果——算法落地后不用改这里任何代码，会自动从"未接入"切到正常显示。

**和弦的语义当时没定**，先按拒绝处理。

---

## 阶段六 · 概率算法讨论（纯讨论，未改代码）

这轮讨论收敛出了实现方案，也纠正了我几个错误说法。

**（一）要的是计数，不是可行性。** 边际概率是 `|{x ∈ S : xᵢ = 1}| / |S|`，LP/ILP 是错的工具。反例：一个已翻开的"1"邻着三格、整盘 1 颗雷，真值 `1/3`，而 LP 松弛的顶点恰好是那三个 0/1 点，单纯形法返回其中一个 → 报告 `p = 1` → 直接触发"无处可躲"的死亡分支。抽象错误直接映射成游戏 bug。

**（二）变量分两类。** 远场格子不出现在任何约束里，只被全局那条支配。不能用 `M/|U|`，也不能用平均消耗——必须先拿到前沿用雷数的完整分布再卷积。

**（三）独立约束免疫行消元。** 行消元要抵消一个共享变量才动得了谁，独立约束没有共享变量，所以碰不到。我一度说"闭式塌缩必须在行消元之前做"，是错的。

**（四）"处境一致"必须是整列相同。** 反例：`a + b = 1` 与 `a + 2b = 1`，第一行里 a、b 系数相同，但解出来 `a ≡ 1`、`b ≡ 0`。判据必须是"在所有行里的系数向量完全相同"。

**（五）代价由接口宽度决定**，不是块大小。并查集等于硬性规定"接口宽度恒为 0"，两块各 18 格共享 1 格时会把 `2^18 + 2^18`（约 52 万）变成 `2^35`（约 340 亿）。

**（六）一条约束单独存在时是多项式**——子集和计数，DP 一遍过。

**术语问题**：这轮我用了一堆自造简写（"闭式塌缩"、"need/free 剪枝"、"接口宽度"），被指出"被各种概念害惨了"。确实：那些步骤本身都很直白，起名字反而让它们看起来像额外机制。

---

## 阶段七 · 求解器实现（v10）

设计收敛到"一个递归 + 两种合并"：

```
solve(rows):
    化简（约 gcd、删空行、用短行抵消长行里的变量）
    断成几块      → 各自 solve，按「方式数相乘、雷数相加」合并
    只剩一行      → 直接枚举
    没有约束      → 常数
    否则          → 挑一格定值，两条路各自 solve，
                    按「方式数相加、分支 1 的雷数平移一格」合并
```

**一行最多 8 格**，所以"只剩一行 → 直接枚举"最多 2⁸ 次，不需要 DP。这个界的来源：初始每一行来自一个已翻开格子的 8 邻域，而化简里的消元只在"抵消后这一行变短"时才保留，所以行永不增长。

**全局那条不进递归**，只在收口时以「前沿用掉 k 颗 × 自由格子的组合数」的形式出现。这样递归里各块是"给定总量"下独立。

**两种合并规则不能混**：并列拆块是**乘**（独立选择），枚举分支是**加**（互斥情形）。搞混了不报错，只会给出错的计数。所以收口处放了 `Σp = M` 的断言。

**数值**：组合数用对数累加再取指数（棋盘上到 40×40 时 `C(1600, 800)` 能到 10⁴⁸⁰，直接乘会溢出）。

---

## 阶段八 · 测试与基准

写了 `js/solver.test.js`，核心是**对拍**：另写一个 `bruteForce()` 把"恰好 M 颗雷且与已翻开数字一致"的布局全部枚举一遍。它不走任何约束求解，是对概率定义的直译，能抓出"合并方式搞混"这类不报错只算错的 bug。

**自己先犯了一个错**：第一版把采样频率拿去和**无条件**概率比。而 `sampleLayout` 返回的是"给定 forbidden 无雷"条件下的布局，频率收敛到的是**条件概率**。这个错误会把一个正确的实现判成失败。改成让 `bruteForce` 支持一个 `forbid` 参数。

**`import` 只能跑一次。** ES module 按 URL 缓存，`import()` 第二次拿到的是缓存、模块体不再执行，所以底部那句 `runAll()` 一页只能触发一次。改成模块体只把入口挂到 `globalThis.solverTest`，由用户反复调用。顺带发现 `passes`/`failures` 是模块级数组，反复调用会累积，加了每次清空。

---

## 阶段九 · 求解器上线后的 UI 问题

**（1）点击后不刷新概率（v11）。**

`act()` 里正常翻开走增量渲染，只重画状态变了的那几格。增量渲染在经典模式下成立，但辅助模式的概率是**整盘耦合**的——全局那条把所有未翻开格子绑在一起，翻开任何一格拿到新数字都会改变其余每一格的条件概率。

判断条件从"有没有重排"换成"是不是辅助模式"。

**（2）不显示概率的格子是绿的，概率为 0 的反而是灰的。**

浓度写成 `sqrt(p) × k`，所以 `p = 0` 时浓度就是 0，格子完全没有着色、露出灰底。而 `p` 小的正数格子反倒绿了。修法是把浓度底数抬起来，让色相承担全部信息表达、浓度只负责"是否显眼"。

**（3）颜色要求更柔和、且不要透明度。**

之前是"半透明色层叠在格子渐变上"，所以底色会透上来把颜色冲淡。改成每个格子直接是一段**实体渐变**，两个色标都由色相算出来。同时压低饱和度、把亮度贴近底色，得到粉彩。

**（4）100% 的格子有框、别的没有（v17）。**

`formatPercent` 在 `99.95%` 就进位到 `100%`，而边框只在**恰好 100%** 才加。于是两格都写"100%"，一格有框一格没框。

两处都改：阈值收紧成"只有真的 100% 才写 100%"（`Math.min(99, Math.round(pct))` 防住中间档）；边框直接去掉——数字已经每格都写，而那圈高饱和硬边和柔和配色不是一个语言。

`p = 1` 在这个模式里是**门槛**（必然有雷、无合法重排），把它和"几乎必然"显示成同一个东西会误导。

**（5）科学计数法导致框大小变化（v19）。**

根因不是宽度是**行高**。基础样式里 `font: 700 23px/1.1 …` 的 `/1.1` 是无单位行高，随字号缩放。科学计数法把字号从 20px 收到 15px，行高从 22px 掉到 16.5px，盒子跟着矮一截。

修法先写死 `line-height`，随后按要求统一所有框的高度，改成把 `height` 本身钉住（`--hud-height`），行高和字号都管不着它。

**（6）移动端自定义栏溢出（v22）。**

`.tools` 有 `flex-wrap: wrap`，但那只让**分组之间**换行；`.group` 自己是 `nowrap`，所以"自定义"是一个不可分割的整块，4 个输入框加按钮横排开，窄屏上直接顶出容器。

修法是给 `.group` 也开 wrap，加 `min-width: 0`。顺带算出 HUD 那一行四个方块至少要 330px 而 320px 屏上可用只有约 270px，也溢出，一起加了断点。

---

## 阶段十 · 重排导致界面卡住（v21）

用户反馈：辅助模式踩到雷、触发重排时 UI 会暂时冻住。

根因是我在写 `sampleLayout` 时选的"逐格条件抽样"——每格要重解两次：

```js
for (const c of frontierCells) {
  const r1 = distWith({ cell: c, value: 1 });   // 完整求解
  const r0 = distWith({ cell: c, value: 0 });   // 再来一次
}
```

前沿 40 格就是 **80 次完整求解**，全同步跑在主线程上。

修法：**一次求解 + 一次下行**。让每个节点在返回时多带一个闭包

```js
sample(k, rng, out)   // 在本节点恰好用掉 k 颗雷的前提下，把取值写进 out
```

四种节点各一行逻辑：叶节点在合法取值里均匀挑；空节点要求 `k === 0`；并列合并按 `A.dist[a]·B.dist[k-a]` 的权重把 k 分给两边；分支合并按 `dist_0[k]` 与 `dist_1[k-1]` 的权重决定走哪支。

代价从 `O(2n × 求解)` 降到 `O(1 × 求解 + 树节点数)`。

**这个 bug 的教训**：我选"逐格条件抽样"是因为它**正确性显然**——照着条件概率的定义直写。但它把一个 O(n) 的算法套在 O(1) 次求解外面，成本是平方级的。而递归结构里本来就有现成信息可用。**"显然正确"和"结构上顺手"之间，我当时只看了前者。**

顺带发现：原有的测试**答不了"会不会卡"这个问题**——对拍和采样验的是结果对不对，而重排的正确性本来就不该变；基准测的是 `computeProbabilities`，卡顿发生在 `sampleLayout`，是另一条代码路径；总耗时又被基准那一节盖住了。于是加了第五节专门测重排，判据是**相对求解的倍数**（同一次求解做分母，不受机器和棋盘大小影响）——应该 ≈ 1，如果变成十几倍就说明退回了老路。

---

## 反复出现的教训

**一、缓存。** 三次事故都和它有关：阶段三的元素崩、`?v=N` 的必要性、测试文件的 `?t=Date.now()`。`python -m http.server` 不发 `Cache-Control`，浏览器会做启发式缓存。

**二、术语堆砌掩盖简单方案。** 阶段六我造了一串名字（"闭式塌缩"、"need/free 剪枝"、"接口宽度"、"分离度"），把直白的步骤包装成额外机制。被指出后重写一遍，同样的方案用"这条约束还差几颗、还有几格没定"就够了。**如果一个步骤需要用专有名词才能说清，通常说明我还没想清楚它到底在做什么。**

**三、渲染粒度必须匹配数据依赖范围。** 只重画"状态变了的格子"在经典模式下对，在整盘耦合的模型下错。这不是缓存问题，是依赖范围问题。

**四、"显然正确"不等于"结构上顺手"。** 见阶段十。

**五、我自己犯的错比外部约束多。** 重复定义的 `announceResult`、重复声明的 `samples`、写死在小函数里的变量、判断错的"不在热路径上"、阈值不一致的 `formatPercent`——这些都不是难点，是没重读自己刚写的东西。

---

## 当前状态

版本 `?v=23`。已知未处理：

- **大棋盘在窄屏上仍会横向滚动**（格子被压到 `MIN_CELL = 12px` 的下限）。这是取舍，不是 bug。
- **`sampleLayout` 的随机源没有接进种子体系**，所以复现一局时重排的结果不可复现（雷图一致，但重排抽到哪一份不一致）。
- **求解器的节点预算（200000）没有任何实测依据**，是估的。
- **所有测试和基准都由用户手动在控制台跑**，没有 CI。
